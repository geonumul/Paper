---
name: paper-to-journal
description: 이미 쓴 논문 원고를 목표 저널(SCI·SCIE) 게재작과 대조해 검수하는 스킬. 원고를 대신 써 주지 않고, 고칠 곳을 근거와 함께 찾아 준다. 낱말·문장·문단·소절·장·전체를 전수로 대조하고, 인용이 실제로 맞는지 원문으로 확인하고, 번역해 둔 두 판이 같은 말을 하는지 맞춰 보고, 그림·표 양식을 게재작과 재서 비교하고, AI 티와 파일 흔적을 없애고, 투고 규격을 점검한다. 트리거: 논문 검수, 원고 검사, 저널 톤 대조, 인용 확인, 번역 대조, 그림 양식, AI 티 제거, 투고 점검, 게재작 실측.
---

# 논문 검수: 게재작을 기준으로 원고를 검사한다

**이 스킬은 논문을 써 주지 않는다.** 이미 쓴 원고를 목표 저널에 실린 논문과
대조해서 **고칠 곳을 근거와 함께 찾아 주는 것**이 전부다. 고치는 것은 저자가
한다.

논문 한 편을 이 방식으로 검수하며 실제로 거친 절차와 그 과정에서 낸 오류를
그대로 옮겼다. 사고의 원본은 `references/12_실패사례집.md`에 있다.

---

## 0. 반드시 받아들일 것 다섯

### ① 기준은 감이 아니라 목표 저널 게재작 실측이다

"이게 더 자연스럽다"로 지적하지 않는다. 지적의 근거는 언제나 **목표 저널에
실제로 실린 논문이 그렇게 쓴다**여야 한다. 재지 않았으면 "재지 않았다"고 말한다.

### ② 원고를 대신 쓰지 않는다

- **한다**: 고칠 곳을 찾아 **왜 고쳐야 하는지 게재작 근거를 붙여** 알린다.
  대체 표현 후보도 **게재작에서 실제로 쓰인 말 중에서** 제시한다
- **안 한다**: 문단을 새로 써 주는 것, 없던 논증·인용·해석을 만들어 붙이는 것,
  문장을 통째로 갈아 주는 것

재료가 전부 실재해도 **연결을 AI가 구성한 순간 티가 난다.** 실제로 논의 절에
그렇게 만든 문단을 넣었다가 통째로 되돌린 적이 있다.

### ③ 기계는 후보만 내고 판정은 사람이 한다

삼항 나열 스캔에서 47건이 걸렸으나 전수 수동 판정 결과 결함은 0건이었다.
기계 경고를 그대로 반영했다면 원고가 나빠졌을 것이다.

**그리고 놓친 것의 1순위 원인이 이것이다.** 직접 읽지 않고 기계나 에이전트에
맡긴 자리, 표본으로 몇 개만 본 자리에서 사고가 났다. 검사를 105회 돌리고도 못
잡은 것을 원문 한 번 열어서 잡았다. **기계가 만든 목록은 "여기까지 봐야
한다"는 뜻이지 "봤다"는 뜻이 아니다**(`16_분석_검증.md` 0항).

### ③-2 절차가 적혀 있으면 그 절차로 한다

**단계를 시작하기 전에 그 단계의 절차 문서를 먼저 연다.** 낱말 층 도구가 잘 돌아간다고 문장 층도 같은 모양일 거라 짐작하고 `15` 4항를 안 읽은 채 임시 스크립트부터 만든 일이 있었다. 4항에는 기능 열 갈래도, 앞뒤 문장을 함께 보라는 지시도, 같은 기능의 게재작 문장을 나란히 놓으라는 절차도 처음부터 적혀 있었다.

그래서 규칙 셋.

1. **임시 스크립트로 절차를 대체하지 않는다.** 스킬의 도구로 못 하는 일이면 도구가 없다고 말하고 멈춘다. 스크래치패드에 만든 것은 그 단계를 한 것이 아니다
2. **갈래 이름을 지어내지 않는다.** 기능은 `15` 4-1항의 **열 가지**, 판정은 4-4항의 **셋**(유지·대체·유보)뿐이다. 지어낸 갈래로 채우면 게재작에서 같은 기능을 찾을 수 없고, 근거가 자리도 기능도 다른 문장이 된다
3. **관문은 빈칸만 보지 않는다.** 무엇으로 채웠는지도 본다. 정해진 이름 밖의 값이 있으면 그 단계는 안 닫힌다

### ③-3 논란이 될 만한 것은 그 층에서 정하지 않는다

**낱말 하나가 걸렸다고 그 자리에서 정하면 틀린다.** 문장에서 걸린 것이 문단에서 풀리고, 문단에서 걸린 것이 소절에서 풀리고, 소절에서 걸린 것이 전체를 보고 나서야 풀린다. 그 반대도 있다. 한 문장에서는 사소해 보이던 것이 전체를 놓고 보면 같은 병이 열두 군데다.

**조금이라도 논란이 될 만하면 판정 칸에 `기록`이라 적고 넘어간다.** `carry_up.py`가 층을 넘어온 것을 `기록_대장.md`에 모으고, 층을 다 지난 뒤 `7e`에서 한꺼번에 정한다. 처리는 셋이다: **고침 / 그대로 둠 / 저자 판단**. 같은 병이 여러 층에 걸쳐 나왔으면 한 줄로 묶어서 정한다.

`기록`은 `유보`와 다르다. **유보는 선례를 못 찾아 저자에게 넘기는 것이고, 기록은 아직 정할 때가 아니라서 위층으로 넘기는 것이다.**

### ④ 시키지 않은 일을 시작하지 않는다

새 스크립트 작성·실행·파일 생성은 **먼저 무엇을 왜 하는지 한두 문장으로 말하고
허락을 받는다.** 읽기만 하는 조회와 이미 시킨 작업의 연장은 그대로 진행한다.

### ⑤ 한 답변에 한 단계만 한다

**이 검수는 며칠에 걸쳐 하는 일이다.** 한 답변에 여러 단계를 끝냈다면 그것은
빨리 한 것이 아니라 **안 한 것이다.**

실제로 전체 검수를 10분 만에 "끝냈다"고 보고한 적이 있다. 대장은 비어 있었고,
판정은 한 행도 없었다. 절차를 글로만 적어 두면 반드시 이렇게 된다.

그래서 **진행 상태를 파일에 두고 관문으로 막는다**(0항-D).

그리고 **검사를 느슨하게 하자고 먼저 권하지 않는다.** 규칙이 비싸 보이면 비싸다는 사실만 말하고, 바꿀지는 저자가 정한다. 스스로 근거를 만들어 몫을 늘리기 시작하면 전수 검수는 그 자리에서 끝난다.

---

## 0-A. 부르면 가장 먼저 묻는 것

**세 가지를 한 번에 묻는다.** 답을 받기 전에는 폴더를 만들지도, 스크립트를
돌리지도 않는다.

> **① 검수할 원고**: 어느 파일입니까. 영문·국문 둘 다 있으면 둘 다 알려 주세요.
>
> **② 가지고 있는 논문 폴더**: 모아 두신 논문이 어느 폴더에 있습니까.
> 없으면 무엇을 받아야 하는지 목록을 만들어 드리겠습니다.
>
> **③ 목표 저널**: 어디에 내실 겁니까. 못 정하셨으면 후보를 찾아 드립니다.
> (검수 기준이 전부 그 저널에서 나옵니다.)
>
> **④ (있으면) 따로 정리해 두신 지침**: 연구실 문체 지침이나 영작 정리본이
> 있으면 알려 주세요. 기준 목록에 넣습니다. 게재작 관행과 충돌하면
> 어느 쪽을 따를지 여쭙겠습니다.

### 처음 쓰는 컴퓨터면 소리 알림을 물어본다

**딱 한 번만 묻는다.** 검수는 오래 걸려서 다른 일을 하다 놓치기 쉽다.

```bash
python scripts/setup_sound.py            # 지금 상태만 본다
```

"없음"으로 나오면 이렇게 여쭙는다.

> 답변이 끝날 때 소리로 알려 드릴까요? (`설정 파일의 hooks 항목만 건드리고,
> 고치기 전에 복사본을 남깁니다. 나중에 --off로 되돌릴 수 있습니다.`)

**승낙하면** `python scripts/setup_sound.py --on`, 물어볼 때도 원하시면
`--notify`를 붙인다. **묻지 않고 설정을 바꾸지 않는다.** 이미 소리가 나고
있으면 건드리지 않고 그대로 둔다. 켠 뒤에는 Claude Code를 껐다 켜야 한다.

### 답을 받으면 바로 하는 것

1. **가진 것을 센다.** 폴더를 실제로 열어 PDF 몇 편, 그중 목표 저널 게재작
   몇 편, 텍스트 캐시가 있는지 **숫자로 보고한다**
2. **저널 기준을 확인한다**(`01_게재작수집_실측.md` 0항). SCI·SCIE 등재인지,
   제외 대상(오픈액세스 전용, 대형 오픈액세스, MDPI·Frontiers 계열, 약탈적
   의심 저널)에 해당하지 않는지
3. **모자라면 받을 목록을 만들어 드린다.** 검색은 우리가 하고 제목·저자·연도·
   링크를 표로 준다. 목표 저널 게재작 최소 40편(문체 대역용, 읽지 않고 기계로 잰다)
4. 받으면 **텍스트를 뽑고 대역을 실측한다**
5. **그 저널이 어느 저널을 몇 % 인용하는지 센다**(`cite_sources.py`).
| `attribution.py <원고> --txt <인용원문폴더>` | **우리 논리가 인용한 저자의 주장처럼 읽히는 자리를 찾는다.** 저자 이름으로 연 문장에서 이어 붙은 마디를 그 원문과 맞춰, 원문에 **0회**인 내용어가 둘 이상이면 올린다. 인용도 진짜고 인용문도 원문 그대로라 되짚기가 못 잡는 갈래다 |
   상위에 있는데 우리가 안 읽은 저널이 곧 추가 수집 대상이다
6. 검수를 시작한다

---

## 0-B. 어떻게 진행하나 (사람이 정하고, 우리는 받친다)

**고칠지 말지는 저자가 정한다.** 우리는 근거를 모아 선택지를 만들어 드리고,
고른 것을 기록하고, 그 결정에 맞춰 나머지를 맞춘다.

### 저자가 정하는 것

- 지적을 반영할지 말지
- 발견을 어느 수위로 말할지
- 무엇을 본문에 두고 무엇을 부록·한계로 보낼지
- 어느 저널에 낼지
- 검수 범위를 어디까지로 할지

### 우리가 하는 것

- **어긋난 자리를 찾는다.** 개수를 세어 보고한다
- **각 지적에 근거를 붙인다.** "이 저널 15편 중 11편이 이렇게 한다"
- **대체 후보를 게재작에서 찾아 준다.** 밖에서 새 말을 가져오지 않는다
- **고른 것을 기록한다.** 안 고치기로 한 것과 그 이유까지

### 무엇을 그냥 고치고, 무엇을 재고, 무엇을 묻나

**전부 대조로 가는 것이 아니다.** 세 갈래로 나눠 다르게 처리한다.

| 갈래 | 어떻게 | 예 |
|--|--|--|
| **그냥 고친다** | 묻지 않고 지적 목록에 올린다. 저널과 무관한 오류다 | 문법 오류, 소수점 자리, 미해결 참조, 표와 본문의 수치 불일치 |
| **재서 정한다** | 저널마다 갈린다. **게재작을 세어 숫자로 제안**하고 저자가 고른다 | we 사용, 이음말 밀도, 문단 길이, 절 제목 형식, 숫자 표기, 참고문헌 개수 |
| **물어본다** | 재도 안 나온다. **저자만 안다** | 주장 강도, 인과로 말할지 상관으로 말할지, 최초 주장 여부, 무엇을 본문에 두고 무엇을 한계로 보낼지 |

**재서 정할 것을 묻지 않고 우리가 고르지 않는다. 물어볼 것을 재서 답하지 않는다.** 갈래별 처리와 제안 형식은 `05_영작규칙.md` 0항.

### 한 단계가 끝날 때마다 이 셋을 보고한다

```
① 한 것      숫자로. "낱말 2,186개 중 2,186개 판정, 대체 63건"
② 정할 것    선택지와 근거. "이 표현을 A와 B 중 어느 것으로 할지"
③ 다음       "정하시면 문장 층으로 넘어갑니다"
```

**②가 없으면 진행하지 않는다.** 정할 것이 없으면 "정할 것 없음"이라고 적는다.

### 매 답변 끝에 진행률 한 줄을 붙인다

지금 전체에서 어디쯤인지 저자가 늘 보이게 한다. **답변마다 빠짐없이 붙인다.**

```
[진행 7/12단계 · 낱말 층 1,204/2,186 (55%) · 남음 982]
```

- 앞은 **전체 12단계 중 몇 번째**(1항의 흐름)
- 뒤는 **지금 층위의 전수 진행**(대장의 행 수 기준). 전수 대상이 없는 단계면
  그 단계에서 할 일 중 몇 개를 끝냈는지
- **모르면 지어내지 않는다.** "낱말 층 미착수"처럼 적는다
- 검사 범위를 좁혔으면 그 사실을 함께: `[진행 7/12 · 낱말·문단만 검사]`

### 대장은 파일로, 대화에는 결론만

낱말·문장·문단 대장을 대화창에 통째로 찍지 않는다. `--out`으로 파일에
쓰고, 대화에는 **개수·판정이 갈린 것·저자가 정할 것**만 남긴다.
**판정은 파일을 열어 전수로 한다.** 줄이는 것은 출력이지 검사가 아니다
(`20_시간과_토큰_아끼기.md`).

### 물을 때의 형식

> **정할 것**: `unassigned`를 그대로 둘지
> **현황**: 목표 저널 36편 중 0편이 이 말을 쓴다
> **A안 유지**: 우리 방법 절에서 뜻이 분명하다. 다만 심사자가 멈출 수 있다
> **B안 대체**: 게재작이 같은 개념을 `unclassified`로 부른다(36편 중 5편)
> **추천**: B안. 다만 정하시는 대로 맞추겠습니다

---

## 0-D. 한 턴에 한 단계 (진행 상태와 관문)

### 매 턴 이렇게 돈다

```
1. 턴을 시작하면      python scripts/progress.py --status
2. 거기 적힌 "이번 턴에 할 일 하나"만 한다
3. 끝나면            python scripts/progress.py --done <단계>
4. 관문을 통과하면 다음 단계로, 못 하면 그 자리에 남는다
5. 답변 끝에 진행률 한 줄을 그대로 붙이고 **멈춘다**
```

처음 시작할 때 한 번만 `--init`을 돌려 진행 상태를 만든다.

```bash
python scripts/progress.py --init 원고.md --journal <저널표식>
```

### 관문이 무엇을 보나

| 단계 종류 | 통과 조건 |
|--|--|
| 물어보는 단계(0, 1) | 저자에게 묻고 **답을 받아** `--note`로 적었는가 |
| 산출물 단계(2-6, 11, 12) | 그 파일이 실제로 있는가 |
| **전수 대장 단계(7a-7d, 7-2, 8, 9, 10)** | **판정이 빈 행이 0인가** |

빈 행이 하나라도 남으면 `--done`이 거부한다. **"대체로 봤다"로는 못 넘어간다.**

### 한 턴 몫

전수 층은 한 번에 다 못 한다. `--status`가 이번 턴에 볼 행 번호를 찍어 준다.

| 층 | 한 턴 몫 |
|--|--|
| 낱말 | 80행 |
| 문장 | 40행 |
| 문단 | 15개 |
| 소절·장 | 5개 |
| 각도별 읽기 | 1회차 |
| 인용 검증 | 10건 |
| 번역 대조 | 10문단쌍 |
| 그림·표 | 2개 |

**그 몫만 하고 멈춘다.** 낱말이 2,193개면 낱말 층만 스물여덟 턴이다. 그것이
정상이다.

### 층마다 무엇을 대야 하는가

**한 층을 다 돌기 전에는 다음 층으로 안 간다.** 그리고 행마다 근거를 댄다.
근거 칸이 비어 있으면 관문이 막는다.

**다섯 층 전부 근거가 붙는다.** 낱말도, 문장도, 문단도, 소절도, 장도다.
근거란 **게재작의 어느 논문 어느 자리인지**를 말한다.

| 층 | 행 수 〔사례〕 | 근거로 대야 하는 것 | 쓰는 도구 |
|--|--|--|--|
| **낱말** | 2,193 | 그 낱말이 게재작 몇 편에 나오는가 **그리고 실제로 쓰인 문장** 하나 이상(어느 논문 어느 자리). 뜻·대상·문법 자리·강도·정의가 우리 쓰임과 같은가 | `corpus_freq.py`, `find_usage.py` |
| **문장** | 779 | **이 문장을 왜 이렇게 썼는가.** 같은 기능을 하는 게재작 문장이 어느 논문 어느 절에 있는가 | `find_usage.py` |
| **문단** | 203 | 메시지 한 줄, 빌드업 순서, 앞 문단과의 연결, **같은 기능을 하는 게재작 문단이 어디인가** | 통독 기록 |
| **소절** | 33 | 이 소절의 목적 한 줄. 게재작의 같은 소절이 어디이고 순서·분량이 어떤가 | `outline_map.py` |
| **장** | 11 | 서두 예고와 소절이 맞는가, 나열인가 연쇄인가. **게재작 같은 장의 배열** | `outline_map.py` |
| **전체** | 5 | 후렴, 구성 예고, 삼중 수치 정합, 용어 일관, 연구 질문과 결론의 대응 | 대장 셋 |

대장은 이렇게 만든다. **판정 칸과 근거 칸을 비워 둔 채로 만들고, 그 빈 칸이
곧 남은 일이다.**

```bash
python scripts/word_census.py 원고.md --txt <코퍼스> --journal <표식> --out outputs/낱말_대장.md
python scripts/unit_census.py 원고.md --sents --out outputs/문장_대장.md
python scripts/unit_census.py 원고.md --paras --out outputs/문단_대장.md
python scripts/unit_census.py 원고.md          --out outputs/소절_대장.md   # 소절·장·전체
```

문장 층의 답은 이 형식이다.

> **문장 4.2-P3-S2**: "The share rose from 29.3% to 49.7% over the period."
> **기능**: 결과 수치 제시
> **근거**: #6 Zhao(2026) 4.3절 두 번째 문장이 같은 자리에서
> "increased from X to Y between A and B" 형식을 쓴다. #12 Lee(2025) 4.1절
> 첫 문장도 같다. 둘 다 **변화량을 괄호로 덧붙이지 않는다.**
> **판정**: 유지. 다만 기간 표기를 게재작 형식(between 2013 and 2023)으로 맞춤

**"게재작에 있는 표현이다"로 끝내지 않는다. 어느 논문 어느 자리인지 적는다.**
적을 수 없으면 그 행은 아직 안 끝난 것이다.

### 이렇게 하면 안 된다

- 한 답변에서 여러 단계를 끝내기
- 대장을 안 만들고 "검사했습니다"라고 말하기
- 판정 칸을 비워 둔 채 완료로 보고하기
- 몫을 넘겨 한 번에 다 판정하기. **도구가 근거를 자동으로 붙여 주더라도
  몫은 그대로다.** 위험은 도구가 못 가른 자리에 몰려 있고, 그 자리는 사람이
  읽어야 보인다. 한 번에 많이 넘기면 읽는 대신 훑게 된다
- **몫을 늘리자고 저자를 설득하기.** 검사가 더디다고 느껴지면 그 사실만
  보고하고 멈춘다. "이러이러하니 300행으로 가는 게 낫다"는 제안을 만들지
  않는다. 검사 강도를 낮추는 결정은 저자만 한다
- 진행률 줄을 손으로 지어내기. **`--status` 출력을 그대로 붙인다**
- 한 층을 다 안 돌고 다음 층으로 넘어가기 (낱말이 남았는데 문장으로 가는 것)
- 근거 칸에 "게재작에 있음"처럼 자리를 안 적기

### 저자가 서두르면

건너뛸 수는 있다. 다만 **건너뛴 것으로 기록한다.**

```bash
python scripts/progress.py --skip 9 --why "국문판이 없어서"
```

보고서에도 "9단계 건너뜀"으로 적힌다. **안 한 것을 한 것처럼 적지 않는다.**

---

## 0-C. 진입 지점 (무엇만 검사할지)

`/paper-to-journal` 뒤에 붙는 말로 검사 범위를 정한다. 없으면 전체 검수로 간다.

| 명령 | 하는 일 | 있어야 하는 것 |
|--|--|--|
| `/paper-to-journal` | 전체 검수 | 원고, 게재작 |
| `/paper-to-journal 수집` | 저널 정하기와 논문 모으기, 대역 실측 | 주제 |
| `/paper-to-journal 기준` | 게재작을 읽어 검수 기준 뽑기 | 게재작 PDF |
| `/paper-to-journal 검수` | 낱말·문장·문단·소절·장·전체 전수 대조 | 원고, 게재작 |
| `/paper-to-journal 인용` | 인용이 실제로 맞는지만 | 원고, 인용한 원문 PDF |
| `/paper-to-journal 번역` | 두 판이 같은 말을 하는지만 | 두 판 원고 |
| `/paper-to-journal 그림` | 그림·표 양식만 | 게재작 PDF, 우리 그림 |
| `/paper-to-journal 국문` | 국문 원고 문체만 | 국문 원고, 기준 국문 논문 |
| `/paper-to-journal 구조` | 목차·절 구성이 게재작과 맞는지만 | 원고, 게재작 |
| `/paper-to-journal 제출` | 투고 규격과 파일 점검만 | 원고, 그림, 투고 규정 |

### 검수만 하러 온 경우 (가장 흔하다)

1. **가진 것을 센다**: 원고, 게재작 몇 편, 텍스트 캐시 유무
2. **게재작이 없으면 그것부터다.** 대역이 없으면 검수의 근거가 없다. 최소
   40편이 필요하다고 먼저 말한다. 급하면 기준 논문 2-4편으로 시작하되
   **"대역 미실측"이라고 기록한다**
3. **기계 검사부터 돌린다.** 여기서 나오는 것이 가장 싸다

   ```bash
   python scripts/check_style.py 원고.md
   python scripts/manuscript_lint.py 원고.md
   python scripts/check_invisible.py 원고.md
   python scripts/check_ngram.py 원고.md
   ```

4. **전수 대장을 만든다**(`word_census.py`, `unit_census.py`). 개수를 먼저
   보고한다: "낱말 2,186개, 문단 203개, 문장 779개"
5. **어디까지 볼지 정하게 한다.** 전체 / 층위 선택 / 급한 것부터
6. 고른 범위는 **끝까지 전수로** 한다. 범위를 좁히는 것은 되지만, 좁힌 범위
   안에서 표본으로 보는 것은 안 된다

**앞 단계 산출물이 없으면 만들지 말고 먼저 알린다.** 건너뛴 단계는 기록에
"건너뜀"으로 남기고, 검수 결과를 보고할 때 함께 적는다.

---

## 1. 전체 흐름

| 단계 | 무엇 | 참고문서 |
|--|--|--|
| 0 | 원고·보유 논문·목표 저널 확인 | 이 문서 0항-A |
| 1 | **저널 정하기**: SCI·SCIE, 제외 대상 거르기 | `01` 0항 |
| 2 | **모으기**: 폴더를 진단하고(`lit_audit.py`) 부족분만 목록으로. 게재작 40편 이상 | `01` 1항 |
| 3 | **실측**: 문장 길이·리듬·이음말·한계 분량·참고문헌 수 | `01` 3-4항 |
| 4 | **인용 출처 분포**: 그 저널이 어느 저널을 몇 % 인용하는가 | `01` 4-2항 |
| 5 | **기준 뽑기**: 게재작을 직렬로 읽고 검수 기준을 만든다 | `02`, `03` |
| 6 | **기계 검사**: 문체·리듬·규격·참조·수치 정합 | `07` 1항 |
| 7 | **전수 대조**: 낱말 → 문장 → 문단 → 소절 → 장 → 전체 | **`15`** |
| 7-2 | **각도별 읽기**: 흐름·맥락·방법·결과·문법·내용·독자 관점으로 읽는다 | **`19`** |
| 8 | **인용 검증**: 원문을 열어 다섯 항목 확인 | `17` |
| 9 | **번역 대조**: 두 판이 같은 말을 하는가 | `18` |
| 10 | **그림·표**: 양식을 수치로 재고 이미지로 보고 대조 | `09` |
| 11 | **AI 티·파일 위생**: 리듬·기호·보이지 않는 문자·익명성 | `08` |
| 12 | **투고 점검**: 규격, 조판, 서약, 파일 목록 | `10` |

**7단계가 본체다.** "이 문장을 왜 이렇게 썼는가"에 "게재작 X의 같은 자리가
이렇게 하고, 우리 자리도 같은 기능이라 그 방식을 따랐다"고 답할 수 있어야
검수가 끝난다.

기록은 전 단계에 걸쳐 남긴다: `11_기록운영.md`.
**분량이 커지면 옛 사실을 지금 사실처럼 말하는 사고가 난다.** 그 방어는
`14_할루시네이션_방지.md`. 사용자를 대하는 방식은 `13_작업방식.md`.
**답장은 항상 "쉽게 설명해줘" 수준으로 한다.**

---

## 2. 언제 어느 문서를 여는가

- 저널을 고르고 논문을 모은다 → `01_게재작수집_실측.md`
- 게재작을 읽고 기준을 뽑는다 → `02_통독_기록형식.md`, `03_규칙서_뽑기.md`
- 원고가 규칙에 맞는지 본다 → `04_검수기준.md`(검수 기준),
  `05_영작규칙.md`(영문), `06_국문문체.md`(국문)
- 검수 순서와 회전 관리 → `07_검수회전.md`
- "AI가 쓴 것 같다"는 지적 → `08_AI티제거.md`
- 그림·표 → `09_그림과표.md`
- 투고 직전 → `10_제출_Elsevier.md`
- 기록을 어디에 남길지 → `11_기록운영.md`
- 같은 실수를 반복한다 → `12_실패사례집.md`(사례집 + 놓친 이유 되먹임)
- 사용자에게 답하는 방식 → `13_작업방식.md`
- 수치·방법·인용을 말하려 한다 → `14_할루시네이션_방지.md`
- **한 단위씩 게재작과 대조하는 방법 → `15_직접대조_검수.md`(본체)**
- 분석에서 무엇을 놓쳤는지 → `16_분석_검증.md`
- **인용이 맞는지만 → `17_인용검증.md`**
- **번역해 둔 두 판 대조 → `18_번역대조.md`**
- **그냥 읽으면서 보는 검사, 회차 운영 → `19_각도별_읽기검사.md`**
- **토큰·시간이 너무 든다 → `20_시간과_토큰_아끼기.md`**
- 단계별 산출물과 점검표 → `00_전체흐름.md`

---

## 3. 어디서든 지키는 표기 규칙

- **em dash(—) 절대 금지.** 하이픈, 쉼표, 마침표로. 대화·문서·원고 전부
- **물결표(~) 금지.** 범위는 하이픈(75-92%), 근사는 "약"
- **지어낸 말 금지, 예외 없음.** 낱말을 조합해 새 말을 만드는 것도 금지.
  방법 조합에 이름 붙이기도 안 한다. 이름 대신 절차로 부른다
- **코드·매개변수 이름은 번역하지 않는다**(`random_state`, `AUC`)
- **은어 금지.** 앵커 → 기준 논문, 씨앗 → 비교 명단
- **소수점 자리 통일**(보통 세 자리)
- **줄임말을 즉석에서 만들지 않는다.** 원고 표현을 그대로 풀어 말한다

---

## 4. 고칠 때마다 지키는 것

1. **고치면 5회 검증한다.** 그 수정 자체를 다섯 번 의심하고, 수정으로 다른
   곳이 상하지 않았는지 전체를 다시 본다. 큰 수정은 10회
2. **파생 파일까지 따라간다.** 원고를 고치면 tex·PDF·서약 문서·그림·저장소
   스크립트가 함께 어긋난다
3. **여파를 본다.** 하이라이트 낱말 하나를 고쳐 글자 수 상한을 넘긴 적,
   초록에서 약어를 빼 정의가 사라진 적이 있다
4. **검증 범위를 좁게 잡지 않는다.** 열 회차를 돌고도 그림 안의 글자를 안 봐서
   절 번호 오류를 놓칠 뻔했다

---

## 4-2. 멈추는 조건 (무한루프 방지)

검수는 그냥 두면 끝나지 않는다. **시작할 때 멈추는 조건을 정하고 기록한다.**

### 고치고 다시 보는 루프 (수정 루프)

| 조건 | 기준 |
|--|--|
| 정상 종료 | 연속 두 번의 재독에서 새 지적 0건 |
| **회차 상한** | 한 층위 **최대 5회**. 넘으면 닫고 남은 것을 "미해결"로 적는다 |
| **같은 자리 반복** | 한 자리를 **세 번** 고치면 멈춘다. 기준이 흔들린다는 신호 |
| **왕복 감지** | A로 고쳤다가 B로 되돌렸다가 다시 A면 멈추고 저자에게 묻는다 |
| **지적이 안 줄어듦** | 세 회차 연속 안 줄면 문장 문제가 아니라 내용 문제다 |
| **범위 이탈** | 원고 밖(추가 분석·새 자료)을 요구하면 그 자리에서 끊는다 |

자세한 것은 `15_직접대조_검수.md` 7-2항.

### 각도를 바꿔 읽는 회차 (읽기 캠페인)

이건 여러 번 도는 것이 정상이다. 100회를 돌든 무제한으로 돌든,
**각도를 바꿔 가며** 돈다(`19_각도별_읽기검사.md` 4항).

- 방식 A(횟수 제한): 정한 횟수까지. 연속 다섯 회차 0건이면 조기 종료
- 방식 B(무제한): 새 지적이 연속 세 회차 0건일 때까지

**둘을 헷갈리지 않는다.** 각도를 바꿔 100번 읽는 것은 정상이고, 같은 문장을
100번 고치는 것은 무한루프다.

### 그리고 하나 더

**렌즈를 늘리지 말고 층위를 올린다.** 같은 층위에서 각도를 바꿔도 새 지적이
안 나오면, 새 각도를 만들 때가 아니라 **층위를 올릴 때**다
(문장 → 문단 → 소절 → 장 → 전체). 실제로 새 렌즈를 계속 만들며 돌다가
"문단·소절·장·전체를 봐야 하는 것 아니냐"는 지적을 받고서야 멈춘 적이 있다.

### 멈출 때 남기는 것

```
낱말 층: 5회차에서 닫음. 미해결 3건(저자 결정 대기)
  - 반복 지적: `unassigned` (3회 고침 → 멈춤, 저자 결정으로)
  - 왕복: 없음
```

**"아직 완벽하지 않다"는 이유로 계속 돌지 않는다.** 남은 것을 적고 넘어간다.

---

## 5. 딸린 도구

프로젝트마다 금지어·폐기 수치가 다르므로 `style_config.json`을 작업 폴더에
두고 읽힌다. 설정이 없으면 저널 무관 기본 규칙만 검사한다.

| 스크립트 | 하는 일 |
|--|--|
| `selfcheck.py` | **도구 자체를 검사한다.** 도구를 고쳤으면 먼저 돌린다(보이지 않는 문자·문법·빈 파일) |
| `progress.py` | **진행 상태와 관문.** 한 턴에 한 단계만 하도록 막는다 |
| `setup_sound.py` | 검사가 끝나면 소리로 알리는 설정을 넣고 뺀다 (묻고 나서만) |
| `lit_audit.py` | **가진 논문 폴더 진단** (편수·못 쓰는 파일·연도·중복·인용했는데 원문 없는 것) |
| `check_ngram.py --extract` | 문헌 PDF를 텍스트로 뽑아 캐시 |
| `check_ngram.py --calibrate [--stability]` | 게재작 문장 리듬 대역 실측. `--stability`는 **몇 편이면 충분한지** |
| `check_ngram.py <원고>` | 5-gram 겹침 + 리듬 대조 |
| `cite_sources.py [--ours <원고>]` | **게재작이 어느 저널을 몇 % 인용하는지.** **한 편이 몇 %씩 인용하는지 편별 중앙값·사분위까지.** `--ours`는 우리 목록을 그 대역에 대 본다 |
| `check_style.py <원고> [--txt <코퍼스>]` | 문체 검사 9종. **`--txt`를 주면 이음말·쉼표 대역을 게재작에서 재서 판정** |
| `manuscript_lint.py <원고>` | 규격·표그림 참조·구성 예고·문단 길이·수치 정합 |
| `word_census.py <원고>` | 낱말 전수 대조표 (국문 지원) |
| `word_census.py <원고> --abbr` | **약어 전수 검사** (원고에서 정의했는가, 게재작이 그 약어를 쓰는가) |
| `collocation.py <원고> --txt <코퍼스>` | **낱말이 아니라 이음을 견준다.** 게재작이 안 쓰는 구문을 우리가 되풀이하는지 |
| `word_judge.py <대장> <시작> <끝> --md <원고> --txt <코퍼스>` | **낱말 대장의 한 구간을 갈래로 갈라 판정하고(`--apply`) 적힌 근거를 되짚는다(`--verify`).** 근거로 갈리는 것만 적고 나머지는 사람에게 남긴다 |
| `sent_judge.py <대장> <시작> <끝> --md <원고> --txt <코퍼스>` | **문장 대장의 한 구간을 `15` 4항 절차로 준비한다(`--apply`).** 기능을 4-1항의 열 갈래 중 하나로 정하고, **같은 기능**의 게재작 문장을 앞뒤와 함께 붙인다. **판정 칸은 비워 둔다.** 논리와 기능은 사람이 읽어야 답이 나온다 |
| `carry_up.py --dir <출력폴더>` | **층에서 `기록`으로 넘긴 것을 한 대장에 모은다(`--apply`).** 문장에서 걸린 것이 문단에서 풀리고 문단에서 걸린 것이 전체를 보고 나서야 풀린다. 정하는 것은 층을 다 지나고 나서다 |
| `unit_census.py <원고>` | 절·문단·문장으로 쪼개 번호 부여 |
| `align_check.py <영문> <국문>` | 두 판의 단위 수·수치·인용·약어 대조 |
| `corpus_profile.py <원고>` | **게재작 대역과 원고를 같은 항목으로 재서 나란히** (리듬·인용 밀도·완곡어·we·쉼표·참고문헌·자기 저널 인용 등) |
| `word_census.py <원고> --floats` | **표·캡션·주석 안의 낱말 전수 대조** |
| `corpus_freq.py <낱말...>` | 용어가 게재작 몇 편에 나오는가 |
| `find_usage.py "패턴"` | 그 표현이 쓰인 문장을 앞뒤와 함께 |
| `outline_map.py <게재작.txt>` | 게재작 목차와 절별 분량 |
| `figure_forensics.py <pdf> <쪽>` | 그림·표의 폰트·색·선 두께 수치 |
| `pdf_text.py <pdf폴더> <출력폴더>` | **PDF에서 글을 뽑는다. 두 단을 단 순서로 읽는다.** 줄 단위로 뽑으면 왼쪽 단과 오른쪽 단이 한 줄씩 섞여 어떤 문장도 안 이어진다. `--check <pdf폴더> <캐시폴더>`로 이미 만든 캐시를 원문과 맞춰 본다 |
| `build_match.py <작업본> <투고물>` | **검수한 것과 내는 것이 같은 글인지 맞춰 본다.** 작업은 마크다운으로 하고 투고는 tex로 짠 PDF를 내면 둘이 갈라진다. 없는 잘못을 고치거나, 고친 것이 투고물에 안 들어간다 |
| `find_source.py <성> <연도>` 또는 `--title <제목 조각>` | **"원문이 없다"고 적기 전에 돌린다.** 프로젝트 안의 모든 PDF·글자 파일을 이름과 속으로 훑고, 스캔본이면 이미지로 읽으라고 말한다. 한 원고에서 "대조 불가"를 네 번 적었다가 네 번 다 뒤집혔다 |
| `pdf_render.py <pdf> <시작> <끝>` | PDF 쪽을 PNG로 |
| `ko_measure.py <국문원고>` | 국문 문장 길이·리듬·종결어미 |
| `_norm.py` | 대조 전 정규화(합자·줄바꿈 붙임표·CRLF·따옴표)와 0건 경고. 다른 도구가 가져다 쓴다 |
| `check_invisible.py <파일> [--fix]` | 보이지 않는 문자와 기호 검사·치환 |

`templates/` 아래 틀이다.

| 틀 | 언제 |
|--|--|
| `시작_기록.md` | 부르면 가장 먼저. 원고·저널·보유 현황·받을 목록 |
| `통독기록_한편.md` | 게재작을 한 편 읽을 때마다 |
| `구조지도.md` | 게재작 구조를 뜯어 우리 원고와 대조할 때 |
| `대조검수_대장.md` | 낱말부터 전체까지 전수 대조를 시작할 때 |
| `그림표_사양서.md` | 그림·표 양식을 재서 채운다 |
| `문제일지_카드.md` | 문제가 하나 생길 때마다 |
| `결정대장_장별.md` | 장별로 정해진 것을 모을 때 |
| `검수회전_계획.md` | 검수 회전을 관리할 때 |
| `검수보고서.md` | **검수를 마치고 저자에게 넘길 한 장 요약** |
| `서약_CRediT.md` | 투고 직전 |

---

## 6. 하지 말 것

- **원고를 대신 쓰지 않는다.** 고칠 곳과 근거를 주는 데까지다
- **전수로 해야 하는 일을 표본으로 하지 않는다.** "몇 개 확인했다"는 안 했다는
  말과 같다. 개수를 세고, 표로 만들고, 모든 행에 판정을 적고, **"N개 중 M개"로
  보고한다**
- **서브에이전트에게 문헌을 배치로 나눠 병렬로 읽히지 않는다.** 실제로 존재하지
  않는 직접 인용문을 만들어 낸 사고가 있었다
- **에이전트 보고를 그대로 믿지 않는다.** 구체적이고 그럴듯한 직접 인용일수록
  원문에서 직접 확인한다
- **"나중에 확인"이라고 적고 넘어가지 않는다**
- **기억을 근거로 대지 않는다.** 게재작 실용례로 판정한다
- **엄밀함을 늘리는 방향으로 표류하지 않는다.** 기준은 "더 엄밀한가"가 아니라
  **"게재작 대비 적절한가"**다

---

## 7. 검사할 때의 규칙 열여섯

한 편을 끝내고 뽑은 것이다. 근거는 `12_실패사례집.md` 항 F.

1. **0이 나오면 도구를 먼저 의심한다.** 특히 답이 너무 균일하면
2. **대조 전에 정규화한다**: 합자, 줄바꿈 붙임표, CRLF, 풀어쓴 숫자, 강세
   (도구는 `scripts/_norm.py`가 처리한다)
3. **0회를 금지 근거로 삼기 전에 표본이 그 갈래를 담는지 본다**
4. **후보를 고르지 말고 전수로 훑는다.** 굳은 내부어는 후보에 안 오른다
5. **하나를 찾으면 같은 종류를 전수 검사한다**
6. **고친 자리를 고친 직후에 다시 검사한다**
7. **바꾸기 전에 우리 원고 안의 다른 자리를 먼저 센다**(과잉 수정 방지)
8. **지표가 말이 안 되면 원고가 아니라 지표를 버린다.** 못 쓸 자는 "보류"
9. **배치는 제약이 먼저, 공간이 나중.** 산출물이 스스로 검산을 찍게 한다
10. **같은 증상이 재발하면 값이 아니라 식을 본다**
11. **검사기 통과는 근거가 아니다.** 인쇄면과 원문을 본다
12. **되는 실물이 있으면 스펙을 읽지 말고 diff한다**
13. **일회성 수선은 재생성 파이프라인에서 죽는다.** 고침은 단계로 넣는다
14. **조용한 실패를 찾아낸다.** 오류를 안 내고 실패하는 도구가 있다
15. **유지 판정도 기록한다.** 다음 회차가 같은 것을 다시 재지 않게
16. **렌즈를 늘리지 말고 층위를 올린다.** 문장 → 문단 → 소절 → 장 → 전체
