---
name: paper-to-journal
description: 이미 쓴 논문 원고를 목표 저널(SCI·SCIE) 게재작과 대조해 검수하는 스킬. 원고를 대신 써 주지 않고, 고칠 곳을 근거와 함께 찾아 준다. 낱말·문장·문단·소절·장·전체를 전수로 대조하고, 인용이 실제로 맞는지 원문으로 확인하고, 번역해 둔 두 판이 같은 말을 하는지 맞춰 보고, 그림·표 양식을 게재작과 재서 비교하고, AI 티와 파일 흔적을 없애고, 투고 규격을 점검한다. 트리거: 논문 검수, 원고 검사, 저널 톤 대조, 인용 확인, 번역 대조, 그림 양식, AI 티 제거, 투고 점검, 게재작 실측.
---

# 논문 검수: 게재작을 기준으로 원고를 검사한다

**이 스킬은 논문을 써 주지 않는다.** 이미 쓴 원고를 목표 저널에 실린 논문과
대조해서 **고칠 곳을 근거와 함께 찾아 주는 것**이 전부다. 고치는 것은 저자가
한다.

논문 한 편을 이 방식으로 검수하며 실제로 거친 절차와 그 과정에서 낸 오류를
그대로 옮겼다. 사고의 원본은 `references/12_실패사례집.md`에 있다.

---

## 0. 반드시 받아들일 것 넷

### ① 기준은 감이 아니라 목표 저널 게재작 실측이다

"이게 더 자연스럽다"로 지적하지 않는다. 지적의 근거는 언제나 **목표 저널에
실제로 실린 논문이 그렇게 쓴다**여야 한다. 재지 않았으면 "재지 않았다"고 말한다.

### ② 원고를 대신 쓰지 않는다

- **한다**: 고칠 곳을 찾아 **왜 고쳐야 하는지 게재작 근거를 붙여** 알린다.
  대체 표현 후보도 **게재작에서 실제로 쓰인 말 중에서** 제시한다
- **안 한다**: 문단을 새로 써 주는 것, 없던 논증·인용·해석을 만들어 붙이는 것,
  문장을 통째로 갈아 주는 것

재료가 전부 실재해도 **연결을 AI가 구성한 순간 티가 난다.** 실제로 논의 절에
그렇게 만든 문단을 넣었다가 통째로 되돌린 적이 있다.

### ③ 기계는 후보만 내고 판정은 사람이 한다

삼항 나열 스캔에서 47건이 걸렸으나 전수 수동 판정 결과 결함은 0건이었다.
기계 경고를 그대로 반영했다면 원고가 나빠졌을 것이다.

**그리고 놓친 것의 1순위 원인이 이것이다.** 직접 읽지 않고 기계나 에이전트에
맡긴 자리, 표본으로 몇 개만 본 자리에서 사고가 났다. 검사를 105회 돌리고도 못
잡은 것을 원문 한 번 열어서 잡았다. **기계가 만든 목록은 "여기까지 봐야
한다"는 뜻이지 "봤다"는 뜻이 아니다**(`16_분석_검증.md` §0).

### ④ 시키지 않은 일을 시작하지 않는다

새 스크립트 작성·실행·파일 생성은 **먼저 무엇을 왜 하는지 한두 문장으로 말하고
허락을 받는다.** 읽기만 하는 조회와 이미 시킨 작업의 연장은 그대로 진행한다.

---

## 0-A. 부르면 가장 먼저 묻는 것

**세 가지를 한 번에 묻는다.** 답을 받기 전에는 폴더를 만들지도, 스크립트를
돌리지도 않는다.

> **① 검수할 원고**: 어느 파일입니까. 영문·국문 둘 다 있으면 둘 다 알려 주세요.
>
> **② 가지고 있는 논문 폴더**: 모아 두신 논문이 어느 폴더에 있습니까.
> 없으면 무엇을 받아야 하는지 목록을 만들어 드리겠습니다.
>
> **③ 목표 저널**: 어디에 내실 겁니까. 못 정하셨으면 후보를 찾아 드립니다.
> (검수 기준이 전부 그 저널에서 나옵니다.)

### 답을 받으면 바로 하는 것

1. **가진 것을 센다.** 폴더를 실제로 열어 PDF 몇 편, 그중 목표 저널 게재작
   몇 편, 텍스트 캐시가 있는지 **숫자로 보고한다**
2. **저널 기준을 확인한다**(`01_게재작수집_실측.md` §0). SCI·SCIE 등재인지,
   제외 대상(오픈액세스 전용, 대형 오픈액세스, MDPI·Frontiers 계열, 약탈적
   의심 저널)에 해당하지 않는지
3. **모자라면 받을 목록을 만들어 드린다.** 검색은 우리가 하고 제목·저자·연도·
   링크를 표로 준다. 목표 저널 게재작 최소 20편
4. 받으면 **텍스트를 뽑고 대역을 실측한다**
5. **그 저널이 어느 저널을 몇 % 인용하는지 센다**(`cite_sources.py`).
   상위에 있는데 우리가 안 읽은 저널이 곧 추가 수집 대상이다
6. 검수를 시작한다

---

## 0-B. 어떻게 진행하나 (사람이 정하고, 우리는 받친다)

**고칠지 말지는 저자가 정한다.** 우리는 근거를 모아 선택지를 만들어 드리고,
고른 것을 기록하고, 그 결정에 맞춰 나머지를 맞춘다.

### 저자가 정하는 것

- 지적을 반영할지 말지
- 발견을 어느 수위로 말할지
- 무엇을 본문에 두고 무엇을 부록·한계로 보낼지
- 어느 저널에 낼지
- 검수 범위를 어디까지로 할지

### 우리가 하는 것

- **어긋난 자리를 찾는다.** 개수를 세어 보고한다
- **각 지적에 근거를 붙인다.** "이 저널 15편 중 11편이 이렇게 한다"
- **대체 후보를 게재작에서 찾아 준다.** 밖에서 새 말을 가져오지 않는다
- **고른 것을 기록한다.** 안 고치기로 한 것과 그 이유까지

### 한 단계가 끝날 때마다 이 셋을 보고한다

```
① 한 것      숫자로. "낱말 2,186개 중 2,186개 판정, 대체 63건"
② 정할 것    선택지와 근거. "이 표현을 A와 B 중 어느 것으로 할지"
③ 다음       "정하시면 문장 층으로 넘어갑니다"
```

**②가 없으면 진행하지 않는다.** 정할 것이 없으면 "정할 것 없음"이라고 적는다.

### 매 답변 끝에 진행률 한 줄을 붙인다

지금 전체에서 어디쯤인지 저자가 늘 보이게 한다. **답변마다 빠짐없이 붙인다.**

```
[진행 7/12단계 · 낱말 층 1,204/2,186 (55%) · 남음 982]
```

- 앞은 **전체 12단계 중 몇 번째**(§1의 흐름)
- 뒤는 **지금 층위의 전수 진행**(대장의 행 수 기준). 전수 대상이 없는 단계면
  그 단계에서 할 일 중 몇 개를 끝냈는지
- **모르면 지어내지 않는다.** "낱말 층 미착수"처럼 적는다
- 검사 범위를 좁혔으면 그 사실을 함께: `[진행 7/12 · 낱말·문단만 검사]`

### 물을 때의 형식

> **정할 것**: `unassigned`를 그대로 둘지
> **현황**: 목표 저널 36편 중 0편이 이 말을 쓴다
> **A안 유지**: 우리 방법 절에서 뜻이 분명하다. 다만 심사자가 멈출 수 있다
> **B안 대체**: 게재작이 같은 개념을 `unclassified`로 부른다(36편 중 5편)
> **추천**: B안. 다만 정하시는 대로 맞추겠습니다

---

## 0-C. 진입 지점 (무엇만 검사할지)

`/paper-to-journal` 뒤에 붙는 말로 검사 범위를 정한다. 없으면 전체 검수로 간다.

| 명령 | 하는 일 | 있어야 하는 것 |
|--|--|--|
| `/paper-to-journal` | 전체 검수 | 원고, 게재작 |
| `/paper-to-journal 수집` | 저널 정하기와 논문 모으기, 대역 실측 | 주제 |
| `/paper-to-journal 기준` | 게재작을 읽어 검수 기준 뽑기 | 게재작 PDF |
| `/paper-to-journal 검수` | 낱말·문장·문단·소절·장·전체 전수 대조 | 원고, 게재작 |
| `/paper-to-journal 인용` | 인용이 실제로 맞는지만 | 원고, 인용한 원문 PDF |
| `/paper-to-journal 번역` | 두 판이 같은 말을 하는지만 | 두 판 원고 |
| `/paper-to-journal 그림` | 그림·표 양식만 | 게재작 PDF, 우리 그림 |
| `/paper-to-journal 국문` | 국문 원고 문체만 | 국문 원고, 기준 국문 논문 |
| `/paper-to-journal 구조` | 목차·절 구성이 게재작과 맞는지만 | 원고, 게재작 |
| `/paper-to-journal 제출` | 투고 규격과 파일 점검만 | 원고, 그림, 투고 규정 |

### 검수만 하러 온 경우 (가장 흔하다)

1. **가진 것을 센다**: 원고, 게재작 몇 편, 텍스트 캐시 유무
2. **게재작이 없으면 그것부터다.** 대역이 없으면 검수의 근거가 없다. 최소
   20편이 필요하다고 먼저 말한다. 급하면 기준 논문 2-4편으로 시작하되
   **"대역 미실측"이라고 기록한다**
3. **기계 검사부터 돌린다.** 여기서 나오는 것이 가장 싸다

   ```bash
   python scripts/check_style.py 원고.md
   python scripts/manuscript_lint.py 원고.md
   python scripts/check_invisible.py 원고.md
   python scripts/check_ngram.py 원고.md
   ```

4. **전수 대장을 만든다**(`word_census.py`, `unit_census.py`). 개수를 먼저
   보고한다: "낱말 2,186개, 문단 203개, 문장 779개"
5. **어디까지 볼지 정하게 한다.** 전체 / 층위 선택 / 급한 것부터
6. 고른 범위는 **끝까지 전수로** 한다. 범위를 좁히는 것은 되지만, 좁힌 범위
   안에서 표본으로 보는 것은 안 된다

**앞 단계 산출물이 없으면 만들지 말고 먼저 알린다.** 건너뛴 단계는 기록에
"건너뜀"으로 남기고, 검수 결과를 보고할 때 함께 적는다.

---

## 1. 전체 흐름

| 단계 | 무엇 | 참고문서 |
|--|--|--|
| 0 | 원고·보유 논문·목표 저널 확인 | 이 문서 §0-A |
| 1 | **저널 정하기**: SCI·SCIE, 제외 대상 거르기 | `01` §0 |
| 2 | **모으기**: 보유분을 세고 부족분만 목록으로. 게재작 20편 이상 | `01` §1 |
| 3 | **실측**: 문장 길이·리듬·이음말·한계 분량·참고문헌 수 | `01` §3-4 |
| 4 | **인용 출처 분포**: 그 저널이 어느 저널을 몇 % 인용하는가 | `01` §4-2 |
| 5 | **기준 뽑기**: 게재작을 직렬로 읽고 검수 기준을 만든다 | `02`, `03` |
| 6 | **기계 검사**: 문체·리듬·규격·참조·수치 정합 | `07` §1 |
| 7 | **전수 대조**: 낱말 → 문장 → 문단 → 소절 → 장 → 전체 | **`15`** |
| 7-2 | **각도별 읽기**: 흐름·맥락·방법·결과·문법·내용·독자 관점으로 읽는다 | **`19`** |
| 8 | **인용 검증**: 원문을 열어 다섯 항목 확인 | `17` |
| 9 | **번역 대조**: 두 판이 같은 말을 하는가 | `18` |
| 10 | **그림·표**: 양식을 수치로 재고 이미지로 보고 대조 | `09` |
| 11 | **AI 티·파일 위생**: 리듬·기호·보이지 않는 문자·익명성 | `08` |
| 12 | **투고 점검**: 규격, 조판, 서약, 파일 목록 | `10` |

**7단계가 본체다.** "이 문장을 왜 이렇게 썼는가"에 "게재작 X의 같은 자리가
이렇게 하고, 우리 자리도 같은 기능이라 그 방식을 따랐다"고 답할 수 있어야
검수가 끝난다.

기록은 전 단계에 걸쳐 남긴다: `11_기록운영.md`.
**분량이 커지면 옛 사실을 지금 사실처럼 말하는 사고가 난다.** 그 방어는
`14_할루시네이션_방지.md`. 사용자를 대하는 방식은 `13_작업방식.md`.
**답장은 항상 "쉽게 설명해줘" 수준으로 한다.**

---

## 2. 언제 어느 문서를 여는가

- 저널을 고르고 논문을 모은다 → `01_게재작수집_실측.md`
- 게재작을 읽고 기준을 뽑는다 → `02_통독_기록형식.md`, `03_규칙서_뽑기.md`
- 원고가 규칙에 맞는지 본다 → `04_집필규칙.md`(검수 기준),
  `05_영작규칙.md`(영문), `06_국문문체.md`(국문)
- 검수 순서와 회전 관리 → `07_검수회전.md`
- "AI가 쓴 것 같다"는 지적 → `08_AI티제거.md`
- 그림·표 → `09_그림과표.md`
- 투고 직전 → `10_제출_Elsevier.md`
- 기록을 어디에 남길지 → `11_기록운영.md`
- 같은 실수를 반복한다 → `12_실패사례집.md`(사례집 + 놓친 이유 되먹임)
- 사용자에게 답하는 방식 → `13_작업방식.md`
- 수치·방법·인용을 말하려 한다 → `14_할루시네이션_방지.md`
- **한 단위씩 게재작과 대조하는 방법 → `15_직접대조_검수.md`(본체)**
- 분석에서 무엇을 놓쳤는지 → `16_분석_검증.md`
- **인용이 맞는지만 → `17_인용검증.md`**
- **번역해 둔 두 판 대조 → `18_번역대조.md`**
- **그냥 읽으면서 보는 검사, 회차 운영 → `19_각도별_읽기검사.md`**
- 단계별 산출물과 점검표 → `00_전체흐름.md`

---

## 3. 어디서든 지키는 표기 규칙

- **em dash(—) 절대 금지.** 하이픈, 쉼표, 마침표로. 대화·문서·원고 전부
- **물결표(~) 금지.** 범위는 하이픈(75-92%), 근사는 "약"
- **지어낸 말 금지, 예외 없음.** 낱말을 조합해 새 말을 만드는 것도 금지.
  방법 조합에 이름 붙이기도 안 한다. 이름 대신 절차로 부른다
- **코드·매개변수 이름은 번역하지 않는다**(`random_state`, `AUC`)
- **은어 금지.** 앵커 → 기준 논문, 씨앗 → 비교 명단
- **소수점 자리 통일**(보통 세 자리)
- **줄임말을 즉석에서 만들지 않는다.** 원고 표현을 그대로 풀어 말한다

---

## 4. 고칠 때마다 지키는 것

1. **고치면 5회 검증한다.** 그 수정 자체를 다섯 번 의심하고, 수정으로 다른
   곳이 상하지 않았는지 전체를 다시 본다. 큰 수정은 10회
2. **파생 파일까지 따라간다.** 원고를 고치면 tex·PDF·서약 문서·그림·저장소
   스크립트가 함께 어긋난다
3. **여파를 본다.** 하이라이트 낱말 하나를 고쳐 글자 수 상한을 넘긴 적,
   초록에서 약어를 빼 정의가 사라진 적이 있다
4. **검증 범위를 좁게 잡지 않는다.** 열 회차를 돌고도 그림 안의 글자를 안 봐서
   절 번호 오류를 놓칠 뻔했다

---

## 5. 딸린 도구

프로젝트마다 금지어·폐기 수치가 다르므로 `style_config.json`을 작업 폴더에
두고 읽힌다. 설정이 없으면 저널 무관 기본 규칙만 검사한다.

| 스크립트 | 하는 일 |
|--|--|
| `check_ngram.py --extract` | 문헌 PDF를 텍스트로 뽑아 캐시 |
| `check_ngram.py --calibrate` | 게재작 문장 리듬 대역 실측 |
| `check_ngram.py <원고>` | 5-gram 겹침 + 리듬 대조 |
| `cite_sources.py` | **게재작이 어느 저널을 몇 % 인용하는지** |
| `check_style.py <원고>` | 문체 검사 9종 |
| `manuscript_lint.py <원고>` | 규격·표그림 참조·구성 예고·문단 길이·수치 정합 |
| `word_census.py <원고>` | 낱말 전수 대조표 (국문 지원) |
| `word_census.py <원고> --abbr` | **약어 전수 검사** (원고에서 정의했는가, 게재작이 그 약어를 쓰는가) |
| `unit_census.py <원고>` | 절·문단·문장으로 쪼개 번호 부여 |
| `align_check.py <영문> <국문>` | 두 판의 단위 수·수치·인용·약어 대조 |
| `corpus_profile.py <원고>` | **게재작 대역과 원고를 같은 항목으로 재서 나란히** (리듬·인용 밀도·완곡어·we·쉼표·참고문헌·자기 저널 인용 등) |
| `word_census.py <원고> --floats` | **표·캡션·주석 안의 낱말 전수 대조** |
| `corpus_freq.py <낱말...>` | 용어가 게재작 몇 편에 나오는가 |
| `find_usage.py "패턴"` | 그 표현이 쓰인 문장을 앞뒤와 함께 |
| `outline_map.py <게재작.txt>` | 게재작 목차와 절별 분량 |
| `figure_forensics.py <pdf> <쪽>` | 그림·표의 폰트·색·선 두께 수치 |
| `pdf_render.py <pdf> <시작> <끝>` | PDF 쪽을 PNG로 |
| `ko_measure.py <국문원고>` | 국문 문장 길이·리듬·종결어미 |
| `_norm.py` | 대조 전 정규화(합자·줄바꿈 붙임표·CRLF·따옴표)와 0건 경고. 다른 도구가 가져다 쓴다 |
| `check_invisible.py <파일> [--fix]` | 보이지 않는 문자와 기호 검사·치환 |

`templates/` 아래 틀이다.

| 틀 | 언제 |
|--|--|
| `시작_기록.md` | 부르면 가장 먼저. 원고·저널·보유 현황·받을 목록 |
| `통독기록_한편.md` | 게재작을 한 편 읽을 때마다 |
| `구조지도.md` | 게재작 구조를 뜯어 우리 원고와 대조할 때 |
| `대조검수_대장.md` | 낱말부터 전체까지 전수 대조를 시작할 때 |
| `그림표_사양서.md` | 그림·표 양식을 재서 채운다 |
| `문제일지_카드.md` | 문제가 하나 생길 때마다 |
| `결정대장_장별.md` | 장별로 정해진 것을 모을 때 |
| `검수회전_계획.md` | 검수 회전을 관리할 때 |
| `검수보고서.md` | **검수를 마치고 저자에게 넘길 한 장 요약** |
| `서약_CRediT.md` | 투고 직전 |

---

## 6. 하지 말 것

- **원고를 대신 쓰지 않는다.** 고칠 곳과 근거를 주는 데까지다
- **전수로 해야 하는 일을 표본으로 하지 않는다.** "몇 개 확인했다"는 안 했다는
  말과 같다. 개수를 세고, 표로 만들고, 모든 행에 판정을 적고, **"N개 중 M개"로
  보고한다**
- **서브에이전트에게 문헌을 배치로 나눠 병렬로 읽히지 않는다.** 실제로 존재하지
  않는 직접 인용문을 만들어 낸 사고가 있었다
- **에이전트 보고를 그대로 믿지 않는다.** 구체적이고 그럴듯한 직접 인용일수록
  원문에서 직접 확인한다
- **"나중에 확인"이라고 적고 넘어가지 않는다**
- **기억을 근거로 대지 않는다.** 게재작 실용례로 판정한다
- **엄밀함을 늘리는 방향으로 표류하지 않는다.** 기준은 "더 엄밀한가"가 아니라
  **"게재작 대비 적절한가"**다

---

## 7. 검사할 때의 규칙 열여섯

한 편을 끝내고 뽑은 것이다. 근거는 `12_실패사례집.md` §F.

1. **0이 나오면 도구를 먼저 의심한다.** 특히 답이 너무 균일하면
2. **대조 전에 정규화한다**: 합자, 줄바꿈 붙임표, CRLF, 풀어쓴 숫자, 강세
   (도구는 `scripts/_norm.py`가 처리한다)
3. **0회를 금지 근거로 삼기 전에 표본이 그 갈래를 담는지 본다**
4. **후보를 고르지 말고 전수로 훑는다.** 굳은 내부어는 후보에 안 오른다
5. **하나를 찾으면 같은 종류를 전수 검사한다**
6. **고친 자리를 고친 직후에 다시 검사한다**
7. **바꾸기 전에 우리 원고 안의 다른 자리를 먼저 센다**(과잉 수정 방지)
8. **지표가 말이 안 되면 원고가 아니라 지표를 버린다.** 못 쓸 자는 "보류"
9. **배치는 제약이 먼저, 공간이 나중.** 산출물이 스스로 검산을 찍게 한다
10. **같은 증상이 재발하면 값이 아니라 식을 본다**
11. **검사기 통과는 근거가 아니다.** 인쇄면과 원문을 본다
12. **되는 실물이 있으면 스펙을 읽지 말고 diff한다**
13. **일회성 수선은 재생성 파이프라인에서 죽는다.** 고침은 단계로 넣는다
14. **조용한 실패를 찾아낸다.** 오류를 안 내고 실패하는 도구가 있다
15. **유지 판정도 기록한다.** 다음 회차가 같은 것을 다시 재지 않게
16. **렌즈를 늘리지 말고 층위를 올린다.** 문장 → 문단 → 소절 → 장 → 전체
