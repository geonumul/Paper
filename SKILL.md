---
name: paper-to-journal
description: 학술지(TFSC·Elsevier 계열 등)에 낼 논문을 목표 저널 게재작 실측에 맞춰 쓰고, 층위별로 검수하고, 투고 파일까지 만드는 전 과정. 목표 저널 논문을 받아 톤·용어·논리 구조를 뽑아내고 그 기준으로 원고를 고쳐 쓸 때, AI 티를 없앨 때, 국문 학술 문체로 옮길 때, 그림·표를 게재작 양식에 맞출 때, CRediT·서약·익명 심사·LaTeX(CAS)·Overleaf 제출 준비를 할 때, 통독 기록·문제 일지·정본 폴더를 운영할 때 쓴다. 트리거: 논문 쓰기, 저널 투고, 원고 검수, 톤 맞추기, TFSC, Elsevier, Overleaf, CRediT, 게재작 실측, AI 티 제거, 통독, 리뷰 대응.
---

# 저널 논문: 실측에서 투고까지

건설 자동화 특허 연구(TFSC 투고본)를 쓰면서 실제로 거친 과정을 그대로 옮긴
것이다. 저널 이름과 수치만 바꾸면 다른 논문에도 쓴다. 각 규칙은 실제로 낸
사고에서 나왔다. 사고의 원본은 `references/12_실패사례집.md`에 있다.

---

## 0. 이 스킬을 쓰기 전에 반드시 받아들일 것 넷

### ① 기준은 감이 아니라 목표 저널 게재작 실측이다

"이게 더 자연스럽다"로 문장을 고치지 않는다. 고치는 근거는 언제나 **목표
저널에 실제로 실린 논문이 그렇게 쓴다**여야 한다. 그래서 이 과정은 글을
고치는 일이 아니라 **재는 일**에서 시작한다. 재지 않았으면 "재지 않았다"고
말한다.

### ② AI가 새 논증·새 인용·새 해석을 만들어 붙이지 않는다

이 스킬 전체에서 가장 중요한 한 줄이다.

> 사실 오류, 수치 오류, 내적 불일치 교정은 계속한다.
> **새 논증, 새 인용, 새 해석을 AI가 만들어 붙이는 일은 하지 않는다.**
> 게재작 선례에 있는 것만 쓰거나, 없으면 저자에게 맡긴다.

재료(인용·앞 절 주장)가 전부 실재해도 **연결 자체를 AI가 구성한 순간 티가
난다.** 실제로 논의 절에 그렇게 만든 문단을 넣었다가 통째로 되돌린 적이 있다.

### ③ 기계는 후보만 내고 판정은 사람이 한다

삼항 나열 스캔에서 47건이 걸렸으나 전수 수동 판정 결과 결함은 0건이었다.
기계 경고를 그대로 반영했다면 원고가 나빠졌을 것이다.

**그리고 놓친 것의 1순위 원인이 이것이다.** 사람이 직접 읽지 않고 기계나
에이전트에 맡긴 자리, 표본으로 몇 개만 본 자리에서 사고가 났다. 검사를 105회
돌리고도 못 잡은 것을 원문 한 번 열어서 잡았다. **기계가 만든 목록은 "여기까지
봐야 한다"는 뜻이지 "봤다"는 뜻이 아니다**(`16_분석_검증.md` §0).

### ④ 시키지 않은 일을 시작하지 않는다

새 스크립트 작성·실행·재계산·파일 생성은 **먼저 무엇을 왜 하는지 한두 문장으로
말하고 허락을 받는다.** "나머지 마저 하자" 같은 말을 스스로 넓게 해석하지
않는다. 읽기만 하는 조회와 이미 시킨 작업의 연장은 그대로 진행한다.

---

## 0-A. 이 스킬을 부르면 가장 먼저 하는 일

**아무것도 시작하기 전에 세 가지를 묻는다.** 답을 받기 전에는 폴더를 만들지도,
스크립트를 돌리지도 않는다. 셋 다 이미 대화에 나와 있으면 확인만 하고 넘어간다.

**진입 지점이 지정된 경우**(`/paper-to-journal 검수` 등)에는 그 단계에 필요한
것만 묻는다(§0-C). 검수만 하러 온 사람에게 연구 질문부터 묻지 않는다.

> **① 주제**: 무엇에 대한 논문입니까. 한 줄로 말씀해 주세요. 연구 질문이
> 정해졌으면 그것도 같이요.
>
> **② 목표 저널**: 어디에 내실 겁니까. **아직 안 정하셨으면 제가 자료와 주제를
> 보고 후보를 추천드리겠습니다.** (기준이 전부 여기서 나옵니다. 저널이 바뀌면
> 문체·용어·그림 양식·분량이 다 바뀝니다.)
>
> **③ 모아 둔 논문**: 이미 모아 두신 논문이 있습니까. 있으면 어느 폴더입니까.
> 없으면 제가 받을 목록을 만들어 드리겠습니다.

물을 때는 한 번에 묻는다. 하나씩 캐묻지 않는다.

### 답을 받으면 바로 하는 것

0. **저널이 안 정해졌으면 추천부터 한다**(`01_게재작수집_실측.md` §0).
   우리가 인용할 논문들이 어디에 실렸는지 세는 것이 가장 강한 신호다. 후보를
   둘셋으로 좁혀 근거와 함께 드리고, **1순위 하나를 고르게 한다.** 후보를 여럿
   둔 채로 진행하지 않는다
1. **현황을 센다.** 폴더를 실제로 열어 PDF가 몇 편인지, 목표 저널 게재작이 몇
   편인지, 텍스트 캐시가 있는지 세어서 **숫자로 보고한다.** "있는 것 같다"로
   넘어가지 않는다
2. **직접 검색해서 받을 논문의 제목 목록을 만들어 드린다**
   (`01_게재작수집_실측.md` §1-3). Claude는 유료·저작권 PDF를 직접 못 받으므로
   받는 것은 사용자가 하지만, **찾는 일은 우리가 한다.** 검색어만 주고 끝내지
   않는다. 저자·연도·제목·저널·링크·왜 받는지를 표로 준다.
   **제목을 기억으로 쓰지 않는다. 검색 결과에 실제로 보인 것만 올린다**
3. 받으면 **편수를 다시 세고**, 텍스트로 뽑고, 대역을 실측한다
4. **읽는 순서를 위험 순으로 정한다**(`02_통독_기록형식.md` §2)
5. **한 편씩 통독하며 기록한다.** 한 편 끝날 때마다 기록을 남기고 다음 편으로
   간다. 몰아 읽지 않는다

시작 기록은 `templates/시작_기록.md`에 남긴다. 주제·저널·보유 현황·받을
목록·통독 진행표가 한 파일에 있다.

### 이미 원고가 있는 경우

원고가 이미 있고 "이 저널 톤에 맞게 고쳐 달라"는 요청이면 위 셋을 묻고 나서
**1-2절의 순서**로 간다.

## 0-B. 어떻게 진행하나 (사람이 정하고, 우리는 받친다)

**연구의 뼈대는 저자가 정한다.** 우리는 근거를 모아 선택지를 만들어 드리고,
고른 것을 기록하고, 그 결정에 맞춰 나머지를 맞춘다. 대신 정해 버리지 않는다.

### 저자가 정하는 것 (우리가 고르지 않는다)

- 연구 질문과 기여를 무엇으로 할지
- 목차와 절 구성
- 어느 저널에 낼지
- 발견을 어느 수위로 말할지
- 무엇을 본문에 넣고 무엇을 부록·한계로 보낼지

### 우리가 하는 것

- **선택지를 만든다.** 게재작에서 실제로 어떻게 하는지 세어서 두셋으로 좁힌다
- **각 선택지에 근거와 득실을 붙인다.** "이 저널 15편 중 11편이 이렇게 한다"
- **고른 것을 기록한다.** 왜 그렇게 정했는지까지(결정 대장)
- **정한 것에 맞춰 나머지를 맞춘다.** 어긋나는 자리를 찾아 알린다

### 한 단계가 끝날 때마다 이 셋을 보고한다

```
① 한 것      숫자로. "게재작 24편 텍스트 추출, 대역 실측 완료"
② 정할 것    선택지와 근거. "목차를 A안(6절)과 B안(5절) 중에서 골라 주세요"
③ 다음       "고르시면 3장 결정 대장을 만들고 집필을 시작합니다"
```

**②가 없으면 진행하지 않는다.** 정할 것이 없으면 "정할 것 없음"이라고 적는다.

### 물을 때의 형식

> **정할 것**: 논의를 결론과 합칠지, 독립 절로 둘지
> **A안 독립 절**: 게재작 15편 중 6편. 질문을 되받을 자리가 생긴다. 쪽수가 는다
> **B안 결론과 합침**: 15편 중 9편(다수파). 짧아진다. 되받을 자리가 줄어든다
> **우리 상황**: 연구 질문이 셋이라 되받을 자리가 필요하다
> **추천**: A안. 다만 정하시는 대로 맞추겠습니다

추천은 하되 **고르는 것은 저자다.** 고른 뒤에는 결정 대장에 적고, 다음
단계부터 그 결정을 기준으로 삼는다.

**결정이 바뀌면** 그 결정에 딸린 것을 전부 다시 본다. 저널이 바뀌면 대역
실측부터, 목차가 바뀌면 구조 지도부터. 바꾼 사실과 이유를 결정 대장에 남긴다.

---

## 0-C. 진입 지점 (어디서부터 시작할지)

`/paper-to-journal` 뒤에 붙는 말로 시작 지점을 정한다. 없으면 전체 절차로
간다. **이미 진행한 단계는 건너뛴다.** 다만 그 단계의 산출물이 있어야 다음
단계가 돌아가므로, 없으면 그 자리에서 알리고 최소한만 채운 뒤 진행한다.

| 명령 | 시작 지점 | 최소 준비물 | 없으면 |
|--|--|--|--|
| `/paper-to-journal` | 0단계부터 전체 | 없음 | - |
| `/paper-to-journal 수집` | 문헌 모으기와 대역 실측 | 주제·목표 저널 | 묻는다 |
| `/paper-to-journal 통독` | 통독과 기록 | 게재작 PDF | 수집으로 돌아간다 |
| `/paper-to-journal 구조` | 구조 해부와 목차 결정 | 통독 기록 | 게재작에서 목차만 뽑아 축약 진행 |
| `/paper-to-journal 집필` | 문단 뼈대와 집필 | 목차 | 구조부터 |
| `/paper-to-journal 검수` | **원고 검수만** | 원고 + 게재작 코퍼스 | 아래 참고 |
| `/paper-to-journal 그림` | 그림·표 양식 | 게재작 PDF, 우리 그림 | - |
| `/paper-to-journal 국문` | 국문 대역 검수 | 국문 원고, 국문 정본 | 정본 없으면 그 사실을 적고 진행 |
| `/paper-to-journal 번역` | **두 판(국문·영문) 대조만** | 두 판 원고 | 한쪽만 있으면 번역 대조는 못 한다 |
| `/paper-to-journal 인용` | **인용이 맞는지만 검사** | 원고 + 인용한 원문 PDF | PDF 없으면 확인 못 한 항목으로 남긴다 |
| `/paper-to-journal 제출` | 투고 파일 준비 | 원고, 그림, 투고 규정 | - |

### 검수만 하러 온 경우 (가장 흔하다)

이미 원고를 쓴 사람이 검수만 받으러 오는 경우다.

1. **가진 것을 센다**: 원고 파일, 게재작 PDF 몇 편, 텍스트 캐시 유무
2. **게재작이 없으면 그것부터다.** 대역이 없으면 검수의 근거가 없다. 최소
   20편이 필요하다는 사실을 먼저 말한다. 급하면 **기준 논문 2-4편만으로
   시작하되 그 사실을 기록에 남긴다**("대역 미실측, 기준 논문 3편으로 대체")
3. **기계 검사부터 돌린다.** 여기서 나오는 것이 가장 싸다

   ```bash
   python scripts/check_style.py 원고.md
   python scripts/manuscript_lint.py 원고.md
   python scripts/check_invisible.py 원고.md
   python scripts/check_ngram.py 원고.md
   ```

4. **전수 대장을 만든다**(`word_census.py`, `unit_census.py`). 개수를 먼저
   보고한다: "낱말 2,186개, 문단 203개, 문장 779개"
5. **어디까지 볼지 정하게 한다.** 전수는 오래 걸린다
   - **전체**: 낱말부터 전체 층까지
   - **부분**: 층위를 골라서(예: 낱말과 문단만)
   - **급한 것부터**: 코퍼스에 없는 낱말, 기계가 걸린 것, 수치 정합만
6. 고른 범위를 **끝까지 전수로** 한다. 범위를 좁히는 것은 되지만, 좁힌 범위
   안에서 표본으로 보는 것은 안 된다

### 중간부터 들어올 때의 공통 규칙

- **앞 단계의 산출물이 없으면 만들지 말고 먼저 알린다.** "대역 실측이 없어서
  리듬은 대역 없이 값만 냅니다"처럼 **무엇이 빠졌고 그래서 무엇을 못 하는지**
  말한다
- 건너뛴 단계는 **기록에 "건너뜀"으로 남긴다**
- 검수 결과를 보고할 때 **어느 단계를 안 거쳤는지 함께 적는다**

---


## 1. 전체 흐름

| 단계 | 무엇 | 참고문서 |
|--|--|--|
| 0 | 준비: 문헌을 갈래별로 나누고 **목표 저널 게재작 폴더를 따로** 둔다. 대역을 실측한다 | `01_게재작수집_실측.md` |
| 1 | 통독: 한 편씩 직렬로, 처음부터 끝까지. 편마다 같은 칸을 채운다 | `02_통독_기록형식.md` |
| 2 | **구조 지도 + 규칙서**: 게재작 목차를 나란히 놓고 **우리 목차를 먼저 정한 뒤**, 글쓰기 관찰에서 14부 규칙서를 만든다. 규칙마다 근거 논문 번호 | `03_규칙서_뽑기.md` |
| 2-2 | **문단 뼈대**: 문단마다 메시지 한 줄과 **문장이 하는 일**을 번호로 쌓아 저자에게 승인받는다 | `04_집필규칙.md` §0 |
| 3 | 집필: 승인된 뼈대대로 쓴다. 문단마다 기록을 남긴다 | `04_집필규칙.md`, `05_영작규칙.md`, `06_국문문체.md` |
| 4 | 기계 검사: 통과 못 하면 다음 층으로 안 간다 | `07_검수회전.md` §1 |
| 5 | **층위별 직접 대조**: 낱말 → 문장 → 문단 → 소절 → 장 → 전체. 단위마다 게재작과 직접 대조하고 수렴할 때까지 재독 | **`15_직접대조_검수.md`**, `07_검수회전.md` §4 |
| 6 | 각도를 바꿔 재순회: 적대적 심사자·인용 감사·어휘 사다리·AI 티·표 자립성 | `07_검수회전.md` §3 |
| 7 | 글 밖: 그림·표 양식을 **수치로 재고 이미지로 보고 베낀다**, 판면, 국문 대역 | `09_그림과표.md`, `06_국문문체.md` |
| 8 | 최종 위생: 보이지 않는 문자, 파일 속성, 익명성 | `08_AI티제거.md` §4 |
| 9 | 제출: 규격 확인, LaTeX·Overleaf, CRediT·서약, 파일 목록 | `10_제출_Elsevier.md` |

단계별 산출물과 한 장 체크리스트는 `00_전체흐름.md`에 있다.
기록은 전 단계에 걸쳐 계속 남긴다: `11_기록운영.md`.
**분량이 커지면 옛 사실을 지금 사실처럼 말하는 사고가 난다.** 그 방어는
`14_할루시네이션_방지.md`에 따로 있다. 수치·방법·인용을 말하기 전에 본다.
사용자를 대하는 방식은 `13_작업방식.md`에 있다. **답장은 항상 "쉽게 설명해줘"
수준으로 한다.**

---

## 1-2. 이미 쓴 원고를 목표 저널 톤으로 다시 쓰는 경우

원고가 이미 있고 "이 저널 톤에 맞게 고쳐 달라"는 요청이면 순서가 다르다.

1. **사용자에게 게재작을 받는다**(`01` §1의 요청 문구를 그대로 쓴다). 최소
   20편. 받은 편수를 세어서 보고한다
2. 텍스트로 뽑아 **대역을 실측한다**(`01` §3-4). 이 숫자가 없으면 고칠 근거가
   없다
3. **원고를 그 대역과 대조한다.** 문장 길이·변동계수·이음말 밀도·한계 분량·
   초록 형식·참고문헌 수. 어긋난 항목만 목록으로 낸다
4. **용어를 반대 방향으로 훑는다**(`01` §7). 원고의 낱말을 코퍼스에 대조하고,
   8% 미만인 것을 골라 **원고 안에 이미 있는 다른 말로** 바꾼다
5. 게재작 몇 편을 **직렬로 통독해 문형과 논리 전개를 채집한다**(`02`).
   서론 여섯 걸음, 갭 형식, 결과 문단의 끝맺음, 한계 절 형식
6. 채집한 것으로 **규칙서를 만들고**(`03`) 그 규칙으로 문단을 고친다(`04`,`05`)
7. **기계 → 층위별 직접 대조 → 각도** 순으로 검수한다(`07`의 순서, `15`의 방법).
   낱말부터 전체까지 단위마다 게재작을 근거로 대고, 새 지적이 안 나올 때까지 재독한다

**고칠 때 새 논증을 만들지 않는다.** 톤을 맞추는 것은 같은 내용을 그 저널이
쓰는 방식으로 다시 쓰는 일이지, 내용을 보태는 일이 아니다.

## 2. 언제 어느 문서를 여는가

- 목표 저널 논문을 새로 받았다 → `01`
- 논문을 읽고 기록을 남긴다 → `02`
- 우리 규칙서가 아직 없다 → `03`
- 문단을 쓴다·고친다 → `04`(구조·문단·수치·금지) + `05`(영문) 또는 `06`(국문)
- 원고를 검수한다 → `07`(순서·회전 관리) + **`15`(한 단위씩 직접 대조하는 방법)**
- "AI가 쓴 것 같다"는 지적을 받았다 → `08`
- 그림·표를 만들거나 판면이 깨졌다 → `09`
- 투고 직전이다 → `10`
- 문제·결정·수치를 어디에 적을지 모르겠다 → `11`
- 같은 실수를 반복하는 것 같다 → `12`(사례집 + **놓친 이유 되먹임** §E)
- 분석·실험에서 무엇을 놓쳤는지 점검한다 → `16_분석_검증.md`
- 사용자에게 답하는 방식 → `13`
- **수치·방법·인용을 말하려 한다, 또는 기록이 많아 헷갈린다 → `14`**
- **인용이 실제로 맞는지만 검사한다 → `17_인용검증.md`**
- **번역해 둔 두 판이 같은 말을 하는지 검사한다 → `18_번역대조.md`**

---

## 3. 어디서든 지키는 표기 규칙

- **em dash(—) 절대 금지.** 하이픈, 쉼표, 마침표로 대체한다. 대화·문서·원고 전부
- **물결표(~) 금지.** 범위는 하이픈(75-92%)이나 "에서", 근사는 "약"
- **지어낸 말 금지, 예외 없음.** 용어는 통독 기록의 "용어" 칸에서 꺼내 쓴다.
  **낱말을 조합해 새 말을 만드는 것도 금지**(`full-candidate value` 같은 것).
  방법 조합에 이름을 붙이는 것도 하지 않는다. 이름 대신 절차로 부른다.
  결합형을 쓰고 싶으면 게재작에 그 형태가 실제로 있는지 먼저 확인한다
- **코드·매개변수 이름은 번역하지 않는다.** `random_state`, `min_cluster_size`,
  `AUC`, `UMAP`은 그대로
- **은어 금지.** 앵커 → 기준 논문, 씨앗 → 비교 기업 명단, 명문화 → 규칙을 글로 적기
- **소수점 자리 통일**(보통 세 자리). 둘로 자르면 0.817과 0.815가 동점이 된다
- **줄임말을 즉석에서 만들지 않는다.** 원고가 "the topology of past
  co-occurrence"라고 쓰면 대화에서도 그대로 풀어 말한다

---

## 4. 고칠 때마다 지키는 것

1. **고치면 5회 검증한다.** 그 수정 자체를 다섯 번 의심하고, 수정으로 다른
   곳이 상하지 않았는지 전체를 다시 본다. 큰 수정은 10회
2. **파생 파일까지 따라간다.** 원고를 고치면 tex·PDF·서약 문서·그림·저장소
   스크립트가 함께 어긋난다. 실제로 세 번 잡혔다
3. **여파를 본다.** 하이라이트에서 낱말 하나를 고쳐 85자를 넘긴 적, 초록에서
   약어를 빼 정의가 사라진 적, 선행사가 없어진 적이 전부 있다
4. **검증 범위를 좁게 잡지 않는다.** 내가 바꾼 곳만 파면 원래 있던 결함을
   못 잡는다. 열 회차를 돌고도 그림 안의 글자를 안 봐서 절 번호 오류를 놓칠
   뻔했다

---

## 5. 딸린 도구

`scripts/` 아래에 있다. 프로젝트마다 금지어·폐기 수치가 다르므로
`style_config.json`을 프로젝트 폴더에 두고 읽힌다. 설정이 없으면 저널 무관
기본 규칙만 검사한다.

| 스크립트 | 하는 일 |
|--|--|
| `outline_map.py <게재작.txt>` | 게재작의 목차와 절별 분량을 뽑는다 (구조 지도 초안) |
| `check_style.py <원고>` | 문체 8종 검사 (금지어·이음말 밀도·약어·소수점·쉼표·번호 연속성·군말·예고 시제) |
| `check_ngram.py --extract` | 문헌 PDF를 텍스트로 뽑아 캐시 |
| `check_ngram.py --calibrate` | 게재작 문장 리듬 대역(CV) 실측 |
| `check_ngram.py <원고>` | 5-gram 겹침 + 리듬 대조 |
| `manuscript_lint.py <원고>` | **규격·표그림 참조·구성 예고·문단 길이·수치 정합을 한 번에 검사** |
| `check_invisible.py <파일> [--fix]` | 보이지 않는 문자 + AI 티 기호(em dash·곡선 따옴표·불릿·이모지) 탐지와 치환 |
| `align_check.py <영문> <국문>` | **두 판의 단위 수·수치·인용·약어를 대조 (번역 검사)** |
| `unit_census.py <원고>` | **원고를 절·문단·문장으로 쪼개 번호를 매긴다 (문장·문단 층 전수 판정용)** |
| `word_census.py <원고>` | **원고의 낱말을 전부 뽑아 코퍼스 대조표를 만든다 (낱말 층 전수 판정용)** |
| `corpus_freq.py <낱말...>` | 게재작 코퍼스에서 용어 빈도 측정 (8% 기준) |
| `find_usage.py "패턴"` | **게재작에서 그 표현이 쓰인 문장을 앞뒤와 함께 뽑아 온다 (직접 대조의 주력 도구)** |
| `figure_forensics.py <pdf> <쪽>` | **게재작 그림·표의 폰트·크기·색·선 두께·점선 패턴을 수치로 뽑는다** |
| `ko_measure.py <국문원고>` | **국문 문장 길이·리듬·종결어미 분포를 재고 정본과 대조한다** |
| `pdf_render.py <pdf> <시작> <끝>` | PDF 쪽을 PNG로 렌더링 (OCR 의심될 때, 게재작 그림 관찰, 조판 확인) |

`templates/` 아래 틀이다.

| 틀 | 언제 |
|--|--|
| `시작_기록.md` | **스킬을 부르면 가장 먼저**. 주제·저널·보유 현황·받을 목록·통독 진행표 |
| `구조지도.md` | 통독을 마치고 **목차를 짜기 전에** |
| `문단뼈대.md` | 목차를 정하고 **문장을 쓰기 전에** |
| `통독기록_한편.md` | 논문을 한 편 읽을 때마다 |
| `문제일지_카드.md` | 문제가 하나 생길 때마다 |
| `결정대장_장별.md` | 한 장을 쓰기 시작하기 전에 |
| `검수회전_계획.md` | 탈고하고 검수를 시작할 때 |
| `대조검수_대장.md` | 낱말부터 전체까지 직접 대조를 시작할 때 |
| `그림표_사양서.md` | 그림·표를 만들기 전에 게재작 양식을 재서 채운다 |
| `서약_CRediT.md` | 투고 직전 |

---

## 6. 하지 말 것 (작업 방식)

- **서브에이전트에게 문헌을 배치로 나눠 병렬로 읽히지 않는다.** 실제로 존재하지
  않는 직접 인용문을 만들어 낸 사고가 있었다. 회사명·시점·내용이 다 들어 있어
  매우 그럴듯했다
- **에이전트 보고를 그대로 믿지 않는다.** 구체적이고 그럴듯한 직접 인용일수록
  원문에서 직접 검색해 확인한다
- **"나중에 확인"이라고 적고 넘어가지 않는다.** 확인 비용은 지금이 가장 싸다
- **기억을 근거로 대지 않는다.** 메모리는 "그렇게 정했다"는 기록이지 "그것이
  옳다"는 근거가 아니다. 게재작 실용례로 정한다
- **읽지 않고 요약을 요약하지 않는다.** 재인용은 반드시 원문으로 확인
- **전수로 해야 하는 일을 표본으로 하지 않는다.** "몇 개 확인했다"는 안 했다는
  말과 같다. 대상 개수를 세고, 표로 만들고, 모든 행에 판정을 적고,
  **"N개 중 M개"로 보고한다**
- **엄밀함을 늘리는 방향으로 표류하지 않는다.** 판단 기준은 "더 엄밀한가"가
  아니라 "게재작 대비 적절한가"다. 목표는 게재이지 통계적 완벽함이 아니다
